{
  "name": "nalartekno-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "sync": "node scripts/sync-wordpress.mjs",
    "prebuild": "node scripts/sync-if-configured.mjs",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "publish": "npm run sync && npm run build",
    "ai:draft": "node scripts/create-draft.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.1",
    "astro": "^7.1.4"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
